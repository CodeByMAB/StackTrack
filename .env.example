# StackTrack Environment Variables

# Bitcoin Price API - Already public, no key needed
# Uses Block's endpoint: https://pricing.bitcoin.block.xyz/current-price

# External APIs
# DO NOT COMMIT YOUR REAL API KEYS TO GIT!
# Copy this file to .env.local to use real keys

# These keys are used through a backend proxy for security
# In a real app, you should never expose API keys in frontend code
VITE_API_PROXY_URL=https://your-backend-proxy.com/api

# For development, set this to "true" to use mock data even if API keys are present
VITE_USE_MOCK_DATA=false